import styles from './Header.module.scss';

const Header = () => {
    return (
        <div className={styles.header} >
            <p className={styles.title}>spli</p>
            <p className={styles.title}>tter</p>
        </div>
    )
}

export default Header;